/** Default GitHub catalog for workspace sibling skills. */
export const WORKSPACE_CATALOG = 'tyohnn/skills';

export const INSTALL_MARKERS = {
  start: '<!-- skill-install:start -->',
  end: '<!-- skill-install:end -->',
};

export const BODY_MARKERS = {
  start: '<!-- skill-body:start -->',
  end: '<!-- skill-body:end -->',
};

export const LOCK_VERSION = 1;
